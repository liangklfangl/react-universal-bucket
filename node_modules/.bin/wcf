#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webpackcc/bin/wcf" "$@"
  ret=$?
else 
  node  "$basedir/../webpackcc/bin/wcf" "$@"
  ret=$?
fi
exit $ret
