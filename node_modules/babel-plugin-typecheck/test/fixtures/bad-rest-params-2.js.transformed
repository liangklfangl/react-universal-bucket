"use strict";

Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.default = test;

function _typeof(obj) { return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj; }

function countArgs() {
	for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
		args[_key] = arguments[_key];
	}

	function _ref(_id) {
		if (!(typeof _id === 'number')) {
			throw new TypeError("Function \"countArgs\" return value violates contract, expected number got " + _inspect(_id));
		}

		return _id;
	}

	if (!(Array.isArray(args) && args.every(function (item) {
		return typeof item === 'number';
	}) || typeof args === 'number')) {
		throw new TypeError("Value of argument \"args\" violates contract, expected Array<number> | number got " + _inspect(args));
	}

	return _ref(args.length);
}

function test() {
	return countArgs();
}

function _inspect(input) {
	return input === null ? 'null' : (typeof input === "undefined" ? "undefined" : _typeof(input)) === 'object' && input.constructor ? input.constructor.name || '[Unknown Object]' : typeof input === "undefined" ? "undefined" : _typeof(input);
}